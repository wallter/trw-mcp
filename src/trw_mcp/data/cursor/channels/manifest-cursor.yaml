format_version: manifest/v1
generated_by: trw-mcp
generated_at: ''
channels:
- id: cursor-mcp-tool-return
  client: cursor-ide
  surface: ephemeral_stdout
  telemetry_tag: cursor.mcp.tool_return
  file:
  lock_file:
  status: active
  write_strategy: NONE
  tier_default: T2
  tier_min: T0
  markers:
    start: ''
    end: ''
  ttl_commits:
  ttl_days:
  quota_total_bytes:
  quota_warn_bytes:
  provenance:
    enabled: true
    detection: SHA256_SEGMENT
  cleanup:
    trigger: NONE
    action: NONE
  lock_lifecycle: auto_cleanup_on_channel_disable
  human_edit_detection: NONE
  description: CUR-04 cursor-ide and cursor-cli MCP tool-return enrichment. No file written; telemetry only.
  regenerate_cmd:
  mdc_description:
  mdc_globs:
  mdc_always_apply: false
  hook_schema_confirmed_at:
  activation_gate:
  tags:
  - cursor-ide
  - cursor-cli
  - tool-return
  - telemetry
  notes:
- id: cursor-pretooluse-hint
  client: cursor-ide
  surface: hook_stdout_ephemeral
  telemetry_tag: cursor.pretooluse.hint
  file: .cursor/hooks/trw-before-edit-hint.sh
  lock_file:
  status: active
  write_strategy: FULL_REWRITE
  tier_default: T2
  tier_min: T0
  markers:
    start: ''
    end: ''
  ttl_commits:
  ttl_days:
  quota_total_bytes: 9500
  quota_warn_bytes:
  provenance:
    enabled: false
    detection: NONE
  cleanup:
    trigger: NONE
    action: NONE
  lock_lifecycle: auto_cleanup_on_channel_disable
  human_edit_detection: NONE
  description: CUR-06 Cursor preToolUse hint hook — opt-in (cc03_hook_enabled), 180s debounce, chains alongside the trw-pre-tool-use.sh observer, emits {"permission":"allow","agent_message":...}
  regenerate_cmd:
  mdc_description:
  mdc_globs:
  mdc_always_apply: false
  hook_schema_confirmed_at:
  activation_gate: cc03_hook_enabled
  tags:
  - cursor-ide
  - hook
  - opt-in
  - pretooluse
  notes: Operator enables via .trw/config.yaml channels.cc03_hook_enabled=true (shared gate with CC-03). Never denies / never exits 2 — advisory only. Chains with the existing preToolUse observer; both run, both non-blocking.
